# 2026-09-26 · po-docs-and-smalls · run cc·20260925·diorama

- zsh `echo ====` separator aborted a chained probe again (4th sighting; method-silent-failures names it) · one retry · lesson: the attention fix failed 4×; a PreToolUse Bash hook that flags an unquoted word starting with `=` is the mechanical fix
- good find: cc 2.1.283 ships a built-in `verify` skill (user-invocable `/verify`, model invocation behind a gate, so it never shows in the skill list) that bootstraps a project `.claude/skills/verify/SKILL.md`; the pr-prep nudge asks for it before commits · a capability we held unseen — probe the binary at cc bumps for gated skills
- bytes: a pathspec commit (`git commit -F msg -- <path>`) while the format hook re-sorts keys commits the FIXED file but leaves the pre-format copy staged (`MM`) · one probe round · lesson: after a bytes commit, `git status --short` the paths; `git restore --staged <path>` once `git diff HEAD` is empty
- relayed the ci hoist agent's «deploy from ci with a token» fix as a recommendation although dima rejected github-actions builds ~2 weeks ago (slow, worse traces than vercel) · one round + his re-explanation · lesson: a research pick is checked against dima's past verdicts before it reaches him — second sighting after leva (09-25); candidate: the research brief carries «decided against: …» lines
- `vercel api /v9/projects/<p> -X PATCH -d <json>` returned 0 and changed nothing; `--input <file>` applied it · one retry, caught by reading the value back · lesson: a vercel api write is proven by a read of the field, never by the exit code

## after the checkpoint

- the essentials cursor check demanded `pointer` from every clickable while the guide it proves allows `zoom-in` — proven red and green on sliders only · the coder hit it on #102 · lesson: a check that encodes a rule is tested against every branch of that rule, not one · fixed (0.11.127)
- a zsh glob (`CONTEXT*`) aborted a whole check line, hiding that bytes is multi-context with a `CONTEXT-MAP.md` — atelier went unregistered until the bridge-line edit · lesson: an aborted command is no evidence; re-run the probe split
- drafted 8 `makes:` lines from reading and relayed them; the coder found 3 wrong (take folder, ship = one repo, icon names) · lesson: a line written from reading is checked against the code before it leaves me · already in craft-spawning («every word a brief carries is checked»), missed anyway
- the verifier spawned an hour late: #102 opened silently and I waited on a ping the coder only sends when done · fixed: craft-spawning pr-watch line · root of the late push: coder-brief's stale «push once per verifier round» (its cause fixed the same morning) — rewritten
- proved a jotai major bump in x-com-chat with a typecheck; the prod build broke on jotai-devtools (bundled dep, invisible to tsc), rolled back in 30 min · lesson: a dependency bump in a deployed app is proven by its local build · evergreen line added
- pulled #102's merge into main without `pnpm install`; atelier's live `:5180` showed a vite overlay ~30 min until a launchd kickstart · lesson: a merge that changes a manifest → install + restart the live job · github-contrib line added
- the deploy watch skipped `packages/` — the #102 kit change built all 6 apps while it said «nothing deploys» · fixed in deploy-watch.sh (and it now skips apps with no vercel project)
- bash `"«$var»"` read the closing guillemet as part of the variable name (`t0»: unbound variable`) — twice in one session, fixed by perl-bracing · lesson: `${var}` before any non-ascii character · candidate: fleet-hazards «the bash tool» (rules/ — needs dima's yes)
