# x-queue

## dima's priorities — his words, 2026-09-13 (approximate map; he unshifts freely; dima's tools roadmap runs alongside)

1. a working ci model: resolve every tail and distractor — ci must work, not interrupt the flow (BYT-95, BYT-96, BYT-100, BYT-94)
2. DOT-26 and the dima's tools roadmap (DOT-26 → DOT-39 → DOT-147 → DOT-237, raycast ≈ half a day)
3. the PO model working — product docs (DOT-244)
4. fully document the sys app (BYT-86)
5. absorb the uncommitted md docs in bytes into sys product docs
6. the x-com-chat ticket combo (BYT-81 → 82 → 37) as the surface for adversary-performance evidence
7. sys as a product with cclio as PO — polish features, keep/discard, solid input for the designer (BYT-75)
8. ~here: enough evidence to decide the adversaries layout (may shift)

## queue

- 🔜 **near:** `/run-skill-generator` on the next bytes coder, or an x-com-chat coder dima drives with cclio assisting (convex + env, its natural first app) · the [BYT-105](https://linear.app/x-com/issue/BYT-105) follow-ups: the drawn atelier logo (cclio art), the loop-encode effort 4 vs 6 measure, two clipped rings (zoom box, settings scroll area). done 09-26: `/verify` explored + the atelier recipe, the gremlins coder (#102, 24 commits), `/run` tried (it routed to the recipe, nothing new)
- 🥇 **the ci one-sweep = [BYT-106](https://linear.app/x-com/issue/BYT-106) (scoped 2026-09-26: order, dima's answers and the exit checklist live in its body; the records fix already landed).** inside it, dima's order 2026-09-12: [BYT-95](https://linear.app/x-com/issue/BYT-95) workflow_run lane + sha-pinned actions → [BYT-96](https://linear.app/x-com/issue/BYT-96) preview lane by label → the product map ([BYT-86](https://linear.app/x-com/issue/BYT-86) draft → designer-skill research → BYT-75) → [BYT-97](https://linear.app/x-com/issue/BYT-97) knip → x-com-chat (BYT-81 → 82 → 37). dima's tools mil order: DOT-26 → DOT-39 → DOT-147 → DOT-237 (sortOrder set). ralph retired; `/goal` trial = [DOT-1](https://linear.app/x-com/issue/DOT-1).
- 🗺️ **the product map** ([FRM-244](https://linear.app/x-com/issue/FRM-244)): atelier pilot ✅ 09-26 → **monday: the designer A/B** (map vs no map, blind pick; plan in FRM-244) → chords → [BYT-86](https://linear.app/x-com/issue/BYT-86) trophy-sys → BYT-75 redesign with the lane measurement
- 🧪 **review-stack measurement — pr 1 scored (bytes #67): matt → ci @claude → coderabbit (greptile dropped 2026-09-25: its free plan runs no cli). pr 2 = BYT-81 decides the local order; then the brief switches to the fallback chain.** sheets: `docs/vet/review-stack.md`.
- 🧬 **evergreen — daily at boot.** read the dependency dashboards too (frame #34, bytes #55: pending-status prs hide there — npm-run-all2, jotai 3). brew + skills lanes weekly. `cmdstats` read ~2026-09-22.
- 🥈 DOT-26 (repo identity: readme, license, purpose) — dima + cclio on fable, his word 2026-09-09: «i want you to take care of dot-26, ask me when ready». then close DOT-39 (last open child). measure day-after: an app-only merge shows 5 skipped previews and no quota move; first ci run on an unchanged package shows `cached` > 0.
- 🔧 x:coder-brief verification contract (dima's yes 2026-09-07, mostly landed in the «final» chain 0.11.47–49; remaining bit) — every done-report line pairs with the command that proved it · red→green for behaviour changes · matt's `code-review` (standards + spec) runs BEFORE the last push: review → fix → push → report «ready»; verdict line in the report · skipped for freebies / one-file PRs. the coder invokes it (opus pays, subagents stay isolated).
- 🥉 trophy-sys redesign, the map: dima's hands (DESIGN-REQUEST §10, commit the three md files) → desktop designer session → comp → impeccable builds on a worktree PR (kit primitives adopted, next-or-vite decided) → `/document` DESIGN.md closes BYT-75. then cv build (BYT-36, comp approved), outpost (BYT-76).
- 💬 **x-com-chat, in order: [BYT-81](https://linear.app/x-com/issue/BYT-81) routes + threads + selector (measurement pr 1) → [BYT-82](https://linear.app/x-com/issue/BYT-82) per-user threads + api guard (measurement pr 2) → [BYT-37](https://linear.app/x-com/issue/BYT-37) instant shell.** bodies written 2026-09-09, dima promoted 81/82 to Todo; he reads them cold, steers, then the opus coder on BYT-81. before the coder: `npx skills update` in bytes (convex skills 5 months stale). tails from the audit: delete `.cursor/rules/` + root `AGENTS.md` pointer + 8 guide lines (coder day); `typescript.ignoreBuildErrors: true` in x-com-chat's next.config.
- 🏢 later, dima 2026-09-08: github org for bytes → blacksmith runners (free org; transfer bytes; re-point vercel ×6, renovate, x-coder-bot, TURBO_TEAM, remotes, linear link; blacksmith gated job in the reflog at 9c25946b). one afternoon of re-pointing, no money.
- 🎯 later: BYT-79 reuse scan (Triage) · BYT-77 payload a/b · BYT-78 storybook · cclio v2 groom (0/4) · DOT-187 (now carries the non-git tool hunt) · BYT-60 name · BYT-44 grill · `@next/playwright` narrow exception when cache components land · stacked prs on the first multi-layer assignment (+1 dima 2026-09-07) · space-explorer-ui: an error boundary, api cancelTrip without validateAuth (noted on #48).
- 🔎 **verifier trial (spec done 2026-09-18, `x:verifier-brief` + DOT-218):** at the next bytes coder spawn — write the ticket's `exit` lines first, spawn the pair, **tamper pr before the real one**; pr 1 opus high, pr 2 sonnet high; the ci opus reviewer stays as the control for both then earns its slot or goes.
- 🥗 **cclio context diet — after the raycast story (dima 09-18):** open with the boot breakdown dima asked for on DOT-215 (106k measured: which slice is memory, skills, tools); then `claude plugin disable x-cw@synced design@synced desktop-commander@synced` (account-synced cw plugins, 87 skill descriptions resident); then the memory reshape — stories + gazette as pointers (trail done), no-op test per leaf, coder-brief incident rules → checklist + script (DOT-238/239 folded here).
- 📬 inbox 3 questions parked readonly 09-18 (`ca` func cleanup, homebrew ui hotkey, linear quicklink searches titles only) → the raycast session answers them.
- 🧪 vets: brew picks 09-15 · coder identity 09-16 (today's comments by `coder` ✅) · initiative 09-17 · proto-lab 10-04 · `x-archive-refresh` after dima's next play.
- 🐞 **ci-watch `--boot` prints `FAIL · could not query gh or vercel` while both answer by hand** (2026-09-19, twice) — the script's probe is the bug; read `cclio/.claude/hooks/ci-watch.sh`'s check, make it name which call failed and why, or drop the FAIL line when the reds list is non-empty (a list is proof the query ran).
- 🧭 **jev flow `coder-retro`** — the fourth vet flow, after flawlog-lanes (dima 2026-09-19, «fold as todo at first free window — codify this») — each retro line laned `memory / rule / ticket / drop` before cclio reads it, rubric in `script/lib/jev-questions.ts`, wired into the flawlog flush; the halt then approves instead of parsing. after that: flowlog-prune «still a wait / ticketed» per line, commit-body banned-keyword + skip-ci marker check, ci-watch red triage.
- ⏰ **the reminders pile — dima's ask 2026-09-21: «review reminders and think/plan to solve them, they're piled up».** 21 entries in `cclio/memory/_reminders.md`, oldest set 2026-08-27, five of them dated vets that have already come due. read all of them cold, then hand him one decision line per entry: due now · retire (its condition is met) · still parked, with the reason · promote to a ticket. 📌 output is a decision list, never edits — a reminder dies only when he drops it. carry into the CST at halt so tomorrow's session opens on it.
