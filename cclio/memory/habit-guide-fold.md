# guide fold — dima's taste asks grow the guide-* skills

dima, 2026-09-26: «let's not create a book of guides there, but good defaults and things i want in
every app — properly working tab navigation, for example». his spotted tweaks (a cursor on a
slider, a boundary per section, a clipped ring) are defaults he wants everywhere, not one-app fixes.

- **the trigger is his ask about how something looks, behaves or is coded** — a screenshot with an
  arrow, «should have», «why doesn't X» — when the fix would be right in any app. one-app taste
  (atelier's dividers out of Tab) stays in that app's recipe, never in a guide.
- **the same turn, one line in the reply**: «🧩 guide fold? `x:guide-ui-ux`: <the rule in one line>» —
  `x:guide-ui-ux` for what a human sees and does, `x:guide-react` for components, `x:guide-typescript`
  for types. he says yes → the line lands, and a check in the `x:browser-headless` essentials when a
  probe can prove it.
- **defaults, not a book**: one line per rule, the positive target, the reason in a clause. a rule
  the essentials already prove names its check, so rule and check never drift.
- today's folds, for the pattern: the cursor set, one tab stop per widget, rings never clipped,
  a boundary per section wrapped from its parent.
